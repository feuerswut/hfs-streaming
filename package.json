{
  "name": "hfs-streaming",
  "private": true,
  "description": "RTMP live streaming plugin for HFS -- ingest via RTMP (OBS / ffmpeg), serve HTTP-FLV with an HTML5 player.",
  "scripts": {
    "build": "node build/build.mjs",
    "build:watch": "node build/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "autoprefixer": "^10.4.20",
    "cssnano": "^7.0.6",
    "esbuild": "^0.24.0",
    "postcss": "^8.4.49",
    "sass": "^1.81.0",
    "typescript": "^5.6.3"
  }
}
